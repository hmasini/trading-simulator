#include "order_book.hpp"

#include <iostream>

int main() {
//     OrderBook book;
//     book.add_order(Order(1, Side::BUY, 100.0, 10, 1));
//     book.add_order(Order(2, Side::BUY, 101.0, 5, 2));
//    book.add_order(Order(4, Side::SELL, 103.0, 6, 4));
//     book.add_order(Order(3, Side::SELL, 102.0, 7, 3));
    

//     book.print_book();

//     std::cout << "\nMatching market BUY order for 5 shares:\n";
//     auto match = book.match_market_order(Side::BUY, 5);
//     if (match) {
//         std::cout << "Matched with order ID: " << match->order_id << "\n";
//     }

//     book.print_book();

    return 0;
}
